# Legacy / referencia

Archivos preservados como referencia visual o de diagnóstico, fuera del bundle de la app.

- `VirtualDeck Brand Icons.html` — catálogo HTML estático de los iconos de marca (origen del set actual en `src/data/brandIcons.ts`).
- `VirtualDeck Icons.html` — catálogo HTML de iconos de acción.
- `test_main.js` — script de diagnóstico de electron usado durante el bring-up inicial.

No son consumidos por el build. Si dejan de servir como referencia, se eliminan.
